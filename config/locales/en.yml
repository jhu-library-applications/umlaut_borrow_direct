en:
  umlaut:
    service_type_names:
      bd_link_to_search:
        one: BorrowDirect Availability
        other: BorrowDirect Availability
      bd_request_prompt: 
        one: BorrowDirect Availability
        other: BorrowDirect Availability
      bd_not_available:
        one: BorrowDirect Availability
        other: BorrowDirect Availability
      bd_request_status:
        one: BorrowDirect Availability
        other: BorrowDirect Availability
    services:
      borrow_direct_adaptor:
        bd_link_to_search:
          display_text: Check BorrowDirect for this item
          notes: ""
        bd_request_prompt:
          pickup_prompt: "Please select a pickup location"
          request: Request
          pickup_validation: "You must choose a pickup location"
          progress: "Checking BorrowDirect availability for you"
        bd_not_available:
          display_text: "This edition is not available via BorrowDirect. You can use the link above to check BorrowDirect manually for other editions."
        bd_request_status:
          progress: "Placing the request to BorrowDirect"
          error:  "There was a problem submitting your request to BorrowDirect."
          confirmation: "Your request has been submitted to BorrowDirect.  You will receive email confirmation shortly. Request number:"
    display_sections:
      borrow_direct:
        title: "Request a copy from BorrowDirect"
        prompt: "Get item from one of our partner libraries in 3-6 days. Checkout period is 12 weeks, with no renewals."
        error: Unable to determine BorrowDirect availability for this edition. 
        error_workaround: Please use the link above to check BorrowDirect for this item manually.

